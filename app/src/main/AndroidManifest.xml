<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medrasoft.pruebamascotas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SeleccionarActividad"
            android:label="@string/title_activity_seleccionar_actividad"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConsultaEliminacion"></activity>
        <activity android:name=".RegistrarVacuna" />
        <activity android:name=".RegistrarPropietario" />
        <activity android:name=".RegistrarMascota" />
    </application>

</manifest>